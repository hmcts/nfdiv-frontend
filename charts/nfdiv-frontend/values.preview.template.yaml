
nodejs:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: divorce-${SERVICE_FQDN}
  registerAdditionalDns:
    enabled: true
    prefix: civil
    primaryIngressHost: ${SERVICE_FQDN}
  environment:

idam-pr:
  enabled: true
  redirect_uris:
    divorce:
      - https://divorce-${SERVICE_FQDN}/oauth2/callback
      - https://civil-${SERVICE_FQDN}/oauth2/callback
