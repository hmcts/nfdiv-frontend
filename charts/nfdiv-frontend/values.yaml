nodejs:
  applicationPort: 3001
  aadIdentityName: nfdiv
  ingressHost: nfdiv-frontend.{{ .Values.global.environment }}.platform.hmcts.net
  image: 'hmctspublic.azurecr.io/nfdiv/frontend:latest'
  keyVaults:
    nfdiv:
      resourceGroup: nfdiv
      secrets:
        - oauth-client-secret
        - AppInsightsInstrumentationKey
        - redis-access-key
  environment:
    REDIS_HOST: "nfdiv-admin-session-storage-{{ .Values.global.environment }}.redis.cache.windows.net"
