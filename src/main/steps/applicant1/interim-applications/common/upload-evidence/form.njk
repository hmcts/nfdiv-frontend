{% from "govuk/components/details/macro.njk" import govukDetails %}
{% from "govuk/components/checkboxes/macro.njk" import govukCheckboxes %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}
{% from "common/macros/service-address.njk" import serviceAddressMacro %}

{% extends "common/form/form.njk"%}

{% block form_content %}

  {% for fieldName, field in form.fields %}
    {% if fieldName == 'applicant1DispensePartnerEmailAddresses' %}
      {% include "common/form/fields/" + field.type + ".njk" %}
      <h2 id="uploadSupportingDocuments" class="govuk-heading-s">{{ uploadSupportingDocuments }}</h2>
    {% elseif fieldName == 'applicant1InterimAppsCannotUploadDocs' %}
      {% include "common/form/fields/" + field.type + ".njk" %}
    {% elseif fieldName == 'applicant1InterimAppsEvidenceUploadedFiles' or fieldName == 'applicant1DispenseNoTraceCertificate'%}
      <p class="govuk-body">
        <strong>{{ acceptedFileFormats }}</strong> {{ fileFormats }}<br>
        <strong>{{ maximumFileSize }}</strong> {{ fileSize }}
      </p>

      {% if not hideUploadAFile %}
        <h2 id="uploadAFile" class="govuk-heading-s">{{ uploadAFile }}</h2>
      {% endif %}
      {% include "common/form/fields/" + field.type + ".njk" %}

      {% include "./document-upload.njk" %}
    {% else %}
      {% include "common/form/fields/" + field.type + ".njk" %}
    {% endif %}
  {% endfor %}

{% endblock %}
