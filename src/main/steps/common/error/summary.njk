{% if errors and errors.length > 0 %}
  {% set errorList = [] %}
  {% for item in errors %}
    {% set errorList = (errorList.push({
      href: item.href,
      text: item.msg
    }), errorList) %}
  {% endfor %}
  {{ govukErrorSummary({
    "titleText": errorSummaryHeading,
    "errorList": errorList
  }) }}
{% endif %}

