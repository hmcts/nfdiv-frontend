{% from "govuk/components/details/macro.njk" import govukDetails %}
{% extends "../common/page.njk" %}

{% block backLink %}{% endblock %}

{% block page_content %}
  <h1 class="govuk-heading-l">{{ title }}</h1>
  {% if completedCaseId %}
    <p class="govuk-body">{{ lineCompleted }}</p>
  {% endif %}
  {% if existingCaseId %}
    <p class="govuk-body">{{ line1 }}</p>
  {% endif %}
  {% if cannotLinkToNewCase %}
    <p class="govuk-body">{{ cannotJoin }}</p>
    {{ govukDetails({
         summaryText: changeExisting.summary,
         html: changeExisting.detail
    }) }}
    {{ govukDetails({
         summaryText: startNewApplication.summary,
         html: startNewApplication.detail
    }) }}
    <p class="govuk-body"><a href="/exit-service" class="govuk-link">{{ exitService }}</a></p>
  {% else %}
    {% if newInviteUserCase %}
      <p class="govuk-body">{{ line2 }}</p>
    {% endif %}
  {% endif %}
{% endblock %}

{% block form %}
  {% include "./form.njk" %}
{% endblock %}
